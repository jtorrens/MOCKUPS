# Sealed historical archive

This directory has no authority over the current MOCKUPS application.

Agents and contributors must not open, search, read, quote, summarize, cite or
use any file in this directory for design, architecture, implementation,
debugging, validation or product decisions unless the user explicitly
authorizes that exact historical consultation.

General instructions to inspect the repository, audit the application, read
documentation, fix a defect or continue development do not grant that
authorization.

Current decisions must come from `AGENTS.md`, the active canonical
documentation outside this directory, the current code, the current schema,
the current Preview manifest, the committed database and executable tests.
